{
  "name": "rate-vinos",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc -b && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist",
    "deploy:netlify": "cross-env NETLIFY=true npm run build",
    "deploy:github": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "canvas-confetti": "^1.9.3",
    "vue": "^3.5.13",
    "vuedraggable": "^4.1.0"
  },
  "devDependencies": {
    "@types/canvas-confetti": "^1.9.0",
    "@vitejs/plugin-vue": "^5.2.1",
    "@vue/tsconfig": "^0.7.0",
    "cross-env": "^7.0.3",
    "gh-pages": "^6.1.1",
    "sass": "^1.85.1",
    "terser": "^5.29.1",
    "typescript": "~5.7.2",
    "vite": "^6.2.0",
    "vue-tsc": "^2.2.4"
  }
}
